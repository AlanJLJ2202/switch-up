import 'package:flutter/material.dart';
import 'package:switch_up/Pages/BodyDetailsSnitch.dart';
import 'BodyDetails.dart';

class DetailsSnitch extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: BodyDetailsSnitch(),
    );
  }
}